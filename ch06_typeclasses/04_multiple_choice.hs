-- Choose correct answers

-- 1. The Eq class:
--   a) includes all types in Haskell
--   b) is the same as the Ord class
-- X c) makes equality tests possible
--   d) only includes numeric types

-- 2. The typeclass Ord
-- X a) allows any two values to be compared
-- X b) is a subclass of Eq
--   c) is a superclass of Eq
--   d) has no instance for Bool

-- 3. Suppose the typeclass Ord has an operator >. What is the type of >?
-- X a) Ord a => a -> a -> Bool
--   b) Ord a => Int -> Bool
--   c) Ord a => a -> Char
--   d) Ord a => Char -> [Char]

-- 4. In x = divMod 16 12
--   a) the type of x is Integer
--   b) the value of x is undecidable
-- X c) the type of x is a tuple
--   d) x is equal to 12 / 16

-- 5. The typeclass Integral includes
-- X a) Int and Integer numbers
--   b) integral, real and fractional numbers
--   c) Schrodinger's cat
--   d) only positive numbers